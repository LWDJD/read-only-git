{
  "name": "read-only-git",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A portable read-only Git browser for static hosts (Arweave / IPFS / Cloudflare Pages / EdgeOne)",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "dev:vite": "vite",
    "prepare-repo": "python scripts/prepare-repo.py",
    "selftest": "node scripts/selftest.mjs"
  }
}
